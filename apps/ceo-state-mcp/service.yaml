apiVersion: v1
kind: Service
metadata:
  name: ceo-state-mcp
  namespace: ceo-state-mcp
  labels:
    app: ceo-state-mcp
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: ceo-state-mcp
  type: ClusterIP
