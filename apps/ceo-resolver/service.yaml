apiVersion: v1
kind: Service
metadata:
  name: ceo-resolver
  namespace: ceo
  labels:
    app: ceo-resolver
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ceo-resolver
