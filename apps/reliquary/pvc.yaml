apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reliquary-backend-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi  # 5 GB allocated for audio records and SQLite DB files
