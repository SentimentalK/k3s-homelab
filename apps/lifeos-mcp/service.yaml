apiVersion: v1
kind: Service
metadata:
  name: lifeos-mcp
  labels:
    app: lifeos-mcp
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: lifeos-mcp
  type: ClusterIP
