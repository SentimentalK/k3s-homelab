apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-ceo-state-mcp
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/SentimentalK/k3s-homelab.git
    targetRevision: master
    path: apps/ceo-state-mcp
  destination:
    server: https://kubernetes.default.svc
    namespace: ceo-state-mcp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
