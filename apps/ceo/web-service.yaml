apiVersion: v1
kind: Service
metadata:
  name: ceo-web
  namespace: ceo
  labels:
    app: ceo-web
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ceo-web
  type: ClusterIP
